# 决策与待定项

这份记录是当前规范的入口。归档文件保留当时的表达，不覆盖本页较新的决定。

## 已确认

| 决定 | 设计影响 |
| --- | --- |
| 产品名首字母大写 Zork | 新界面不使用早期小写字标 |
| Device → Leader → Task | Worker 不与 Leader 并列成为人类聊天入口 |
| Task 用群聊式内容组织 | 显示参与者、消息来源与评论 |
| 选中文字后逐条评论，最后一次发送 | 评论浮窗不直接发送；队列按对话保存 |
| 统一桌面所有导航行 | 30 px、同宽 hover / active、统一圆角 |
| active 与未读不改字重 | 避免点击后文字和布局抖动 |
| 去掉 outline 和头像外圈 | 保留背景式键盘焦点反馈 |
| 输入框自动长高 | 无手动 resize 把手 |
| 侧栏固定像素、可拖动、保存宽度 | 聊天与设置一致 |
| 会话头收紧，头像左对齐 | 44 px 与共享消息列 |
| 设备下放 Agent 与模型连接 | 设置归属明确 |
| 接入方式优先，再选供应商 | 紧凑带品牌图标的下拉 |
| 连接后管理模型 | 新连接没有伪造的预设模型 |
| 本地预览 | 当前整理不包含部署或发布 |

## 移动端已确认

移动端 nav7：导航与对话独占一屏；导航行 48 px；按压背景整行同宽；聊天列表不保留 active；底部两个入口并排等宽；从设备子页可直接返回原对话并保留草稿和阅读位置。此处是平台适配，不覆盖桌面的常驻当前对话状态。

## 当前方向

折角伙伴、炭墨/暖纸/柿橙、12 个动物头像、轻量功能图标。它们已用于交互样稿，并交给原生实现；平台各尺寸仍需检视。

## 仍待评审

- **定制字标**：不能只是普通字体排版；早期橙色块未清楚呈现折叠感；v2 改为轮廓折口，并保留回转候选。
- **品牌动画**：已提供图标、字标、联动、图标变名字四套 SVG 动效，细节随字标定稿。
- **宣传语**：中文与英文都是提案。
- **深色主题**：只有基础配色提案，尚未完成组件校验。
- **App icon**：已有 SVG 母稿与分层样张，尚未完成全部平台打包与尺寸验收。
- **场景插画**：已有素材，是否使用及摆放位置按实际页面决定。

## 已被替代

全小写 Inter 排版字标、选中导航的橙色竖条、靠加粗表达 active/未读、供应商全部平铺、连接时塞入模型预设、设置右侧重复主标签、各层级不同宽 hover、旧 task-first HTML。

## 设计与实现状态

原生 GPUI 实现已合回 mini1 主代码目录，并完成 62 项 GUI 测试（2 项既有外部 fixture 忽略）、1280×800 / 900×600 窗口回归，以及四种动效正常/减少动态模式的像素检查。后台接口回归也已通过。当前字标、语义图标与有真实承载位的场景已接入；正式 App 安装未替换。

详细挂载位置与帧证据见 [原生接入记录](../implementation/README.md)。历史网页样稿不作为原生窗口验收证据。

## 2026-09-07 · 控件视觉更新

采用胶囊按钮、12 px 表单控件、32 px 弹窗与评论浮层、淡边框、20 px select 展开层；展开层加宽以保留选项留白并对齐内容。模型表单采用字段级错误提示，历史记录采用紧凑行。详细规则见 [控件视觉定稿](14-controls-revision.md)。
