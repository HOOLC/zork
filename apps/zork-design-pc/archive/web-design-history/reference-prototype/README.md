# Historical product prototype

Preserved during the monorepo migration on 2026-09-09 from the last `zork-design/dist/reference-prototype` copy. The former `zork/artifacts/brand-review-site/src` source directory no longer existed. This snapshot keeps the original prototype, scripts and assets available without a sibling checkout or local artifacts path. It is historical design evidence; current React references and shared GPUI components remain separate.
